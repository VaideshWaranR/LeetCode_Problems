class Solution {
    public String defangIPaddr(String address) {
        String res="";
        for(int i=0;i<address.length();i++){

        if(address.charAt(i)=='.')

           res=res.concat("[.]");

else

     res=res.concat(String.valueOf(address.charAt(i)));

      }
        return res;
    }
}